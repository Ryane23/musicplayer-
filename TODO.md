# TODO - Ensure each functionality works

- [ ] Fix expo-router tab layout route mismatch for `library`
- [ ] Replace broken sample audio URIs (avoid 404) with known-good URLs
- [ ] Update MusicLibraryService to gracefully handle missing Android permissions (return empty list instead of throwing)
- [ ] Improve MusicPlayerService error handling so UI/state stays consistent when createAsync fails
- [ ] (After code changes) Run `npm run lint`
- [ ] (After code changes) Smoke test playback: play/pause, next/prev, seek, volume

